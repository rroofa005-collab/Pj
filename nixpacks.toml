[phases.setup]
nixPkgs = ["nodejs_20", "python3", "make", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install --build-from-source"]

[phases.build]
cmds = ["npm run build"]

[vars]
NODE_ENV = "production"